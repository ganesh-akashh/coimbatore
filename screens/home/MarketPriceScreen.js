import { View, Text, SafeAreaView, StatusBar } from 'react-native'

const MarketPriceScreen = () => {
    return (
        <SafeAreaView className="flex-1 bg-[#FFFFFF] flex space-y-1">
           
        </SafeAreaView>
    )
}

export default MarketPriceScreen